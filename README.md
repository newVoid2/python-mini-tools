# python-mini-tools

Small, self-contained Python scripts that each do one simple thing:
- practice using the core language
- build muscle memory
- keep my GitHub active while I go through ZTM

Each file in `tools/` is a standalone mini-tool.
